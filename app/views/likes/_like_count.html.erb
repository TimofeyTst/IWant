    
<%= turbo_frame_tag "#{dom_id(comment)} public_likes" do %>
    <% if comment.likes.any? %>
        <div class="counter">
            <%= (t "reaction", count: comment.likes.count) %>
        </div>    
    <% end %>
<% end %>